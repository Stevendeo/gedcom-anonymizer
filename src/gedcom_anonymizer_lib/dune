; generated by drom from package skeleton 'library'

(library
  (name gedcom_anonymizer_lib)
  (public_name gedcom_anonymizer_lib)
  (wrapped true)
  (libraries gedcom )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package gedcom_anonymizer_lib))


